---
- job:
    name: my-first-job
    node: master
    description: First job creation
    logrotate:
      daysToKeep: 30
      artifactDaysToKeep: 1
    pipeline-scm:
      script-path: pipeline-scripts/my-first-job.groovy
      parameters:
        - choice:
            name: Environment
            choices:
              - development
              - Test
              - Integration
              - PreProd
