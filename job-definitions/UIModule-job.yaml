-job:
  name: UIModule-job.yaml
  node: master
  description: First job creation
  logrotate:
    daysToKeep: 30
    artifactDaysToKeep: 1
    parameters:
      - choice:
          name: Environment
          choices:
            - development
            - Test
            - Integration
            - PreProd
